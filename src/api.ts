export type PlanApi = {
	_id: string;
	title: string;
	price: number;
	annuality: string;
	image: string;
};

export type AddonApi = {
	_id: string;
	title: string;
	content: string;
	price: number;
	annuality: string;
};
